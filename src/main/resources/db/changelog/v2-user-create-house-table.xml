<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.15.xsd">

    <changeSet id="003-create-house-table" author="anshu">

        <!-- ========================= -->
        <!-- Create house Table -->
        <!-- ========================= -->

        <createTable tableName="house" schemaName="users">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"
                             primaryKeyName="pk_house"
                             nullable="false"/>
            </column>

            <column name="uuid" type="UUID">
                <constraints nullable="false"
                             unique="true"
                             uniqueConstraintName="uk_house_uuid"/>
            </column>

            <column name="name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="owner_uuid" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at"
                    type="TIMESTAMP"
                    defaultValueComputed="CURRENT_TIMESTAMP"/>

            <column name="updated_at"
                    type="TIMESTAMP"
                    defaultValueComputed="CURRENT_TIMESTAMP"/>

        </createTable>

        <!-- ========================= -->
        <!-- Index -->
        <!-- ========================= -->

        <createIndex indexName="idx_house_owner"
                     tableName="house"
                     schemaName="users">
            <column name="owner_uuid"/>
        </createIndex>

        <!-- ========================= -->
        <!-- Check Constraint (Stable SQL Way) -->
        <!-- ========================= -->

        <sql>
            ALTER TABLE users.house
            ADD CONSTRAINT chk_house_status
            CHECK (status IN ('ACTIVE','INACTIVE'));
        </sql>

    </changeSet>

</databaseChangeLog>
