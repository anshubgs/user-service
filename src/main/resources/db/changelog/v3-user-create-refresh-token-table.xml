<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.15.xsd">

    <changeSet id="004-create-refresh-token" author="anshu">

        <!-- ========================= -->
        <!-- Create refresh_token Table -->
        <!-- ========================= -->

        <createTable tableName="refresh_token" schemaName="users">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"
                             primaryKeyName="pk_refresh_token"
                             nullable="false"/>
            </column>

            <column name="token" type="VARCHAR(500)">
                <constraints nullable="false"
                             unique="true"
                             uniqueConstraintName="uk_refresh_token_value"/>
            </column>

            <column name="user_uuid" type="UUID">
                <constraints nullable="false"/>
            </column>

            <column name="expiry_date" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="revoked" type="BOOLEAN"
                    defaultValueBoolean="false"/>

        </createTable>

        <!-- ========================= -->
        <!-- Index -->
        <!-- ========================= -->

        <createIndex indexName="idx_refresh_user"
                     tableName="refresh_token"
                     schemaName="users">
            <column name="user_uuid"/>
        </createIndex>

        <!-- ========================= -->
        <!-- Foreign Key -->
        <!-- ========================= -->

        <addForeignKeyConstraint
                baseTableSchemaName="users"
                baseTableName="refresh_token"
                baseColumnNames="user_uuid"
                referencedTableSchemaName="users"
                referencedTableName="user_account"
                referencedColumnNames="uuid"
                constraintName="fk_refresh_token_user"/>

    </changeSet>

</databaseChangeLog>
